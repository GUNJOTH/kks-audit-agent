# 安全策略

## 不要提交的内容

不得提交 API Key、管理令牌、数据库密码、Cookie、私钥、真实生产 Excel、个人信息、未脱敏审核报告、内部服务地址或包含敏感请求头的日志。

## 报告漏洞

发现未授权访问、管理接口暴露、凭据泄露、文件上传绕过或敏感数据外发时，不要创建公开 Issue 或 Pull Request。优先使用 GitHub Private vulnerability reporting；未启用时请通过维护者私密渠道报告，并只提供脱敏复现信息。

## 运行时建议

生产环境应配置随机 KKS_ADMIN_TOKEN，通过反向代理或受控内网提供服务，限制上传大小和文件保存目录，轮换 AI/数据库凭据，并审计配置修改、Skill 上传和 AI 测试操作。